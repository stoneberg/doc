




1.승인업무는 JOB에 해당된다.

JOB 은 3가지 분류로 나뉘는데

1. 직렬 JOB   : 대기 -> 진행 -> 완료
2. 병렬 ALL JOB : 대기 -> 진행 -> 완료
3. 병렬 1 JOB : 대기 -> 완료


======================================================

현재 리스트 상 표기될 정보는 JOB의 진행상태(flow code)와 결과(result code)다.

flow code -> progsSttus
result code -> approvalSttus


직렬 JOB 이나 병렬 ALL JOB 같은 경우는 STEP을 거쳐야 하기 때문에
개인별로 result code를 가지수도 있겠지만, 사실 approvalType 정보와 실제로 결제할 총인원 및 결제된 인원만 있으면
위의 정보를 산출할 수 있다.

다만, 개인이 결제를 했는지 않했는지 바로 알 수 있기 위해서 개인별로 결과 코드를 가진다고 볼수 있다.



































